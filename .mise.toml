# .mise.toml — Skein project runtime versions
# https://mise.jdx.dev/configuration.html
#
# mise manages Erlang/OTP and Elixir versions for this project.
# Run `mise install` to set up, or let the SessionStart hook handle it.

[tools]
erlang = "28.1"
elixir = "1.19.5-otp-28"

# No [env] section: a forced MIX_ENV here overrides the caller's value
# for every shimmed command, silently turning MIX_ENV=prod release
# builds into dev builds. Mix already defaults MIX_ENV to dev.
